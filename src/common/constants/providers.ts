export const HIVE_PARSER_PROVIDERS = Object.freeze({
  MAIN: 'HiveParserDomain',
});

export const REDIS_PROVIDERS = Object.freeze({
  MAIN: 'RedisClient',
});

export const PYRAMIDAL_BOT_PROVIDERS = Object.freeze({
  MAIN: 'PyramidalBotDomain',
});
